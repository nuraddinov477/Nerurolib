{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && pip install -r backend/requirements.txt && cd backend && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "cd backend && python manage.py migrate --noinput && gunicorn bookstore.wsgi --bind 0.0.0.0:$PORT --workers 1 --timeout 120",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
